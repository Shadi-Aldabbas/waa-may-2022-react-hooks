

function Product (props){
        return (
            <div>
                    <h1>{props.name}</h1>
                    <h4>{props.price}</h4>
                </div>
        )
};

export default Product;